// List<Contact> contacts = List.empty(growable: true);

class Contact {
  String name;
  String number;
  Contact({required this.name, required this.number}) {}
}

// List<Contact> contacts = List.empty(growable: true);
